---
description: What's still open in the webapp: an unverified renderer question about multi-position links, deep-write hand-off left unexamined, and a ctrl-click gesture bug that contradicts the settled design.
---

# Webapp — Issues <a name='id/BlockNode:00CJ6V6370001' class='aperas-anchor aperas-id'></a>

## Open Issues <a name='id/BlockNode:00CJ6V6370002' class='aperas-anchor aperas-id'></a>

- **Whether the tree renderer surfaces every position of a multi-position `Link`**: <a name='id/BlockNode:00CJ6V6370003' class='aperas-anchor aperas-id'></a> the graph carries one `position` prop per occurrence, and the endnote's `^ a b` back-references need one anchor per position. Whether `renderTreeWithView` emits such a link once or once per position is unchecked. A renderer question, not a model one, and it must be answered before the endnote can claim to be faithful.
- **How a deep write is handed to the Agent**: <a name='id/BlockNode:00CJ6V6370004' class='aperas-anchor aperas-id'></a> composing an intent and handing it off — a copied prompt, an MCP endpoint, an embedded chat pane — is unexamined, and deliberately out of Phase 1. It is deferred rather than rejected, and nothing in Phase 1 should foreclose any of the three.
- **Ctrl-click on the fold arrow folds instead of zooming, and swallows the event**: <a name='id/BlockNode:00CMWN0XDG001' class='aperas-anchor aperas-id'></a> `FolderDiv.tsx`'s `Gutter` is the only click site in that file which never checks `ctrlKey`/`metaKey` — the node line, both link-row modes and the backlinks popover all do — and because its handler calls `e.stopPropagation()`, the row's own zoom handler never sees the event either. So ctrl-clicking the arrow silently folds and the zoom is simply lost, which reads as "zoom doesn't work" to anyone who aims at the control rather than at the title; found by a user hitting it on first use of the app. This contradicts the settled design rather than filling a gap in it: [zoom in is a new apex](../discussion/webapp.md#id/BlockNode:00CJ2V1YAR009) specifies the gesture on the arrow itself ("ctrl-clicking a fold arrow re-renders with that node as apex"), so the implementation has never matched what was agreed. Fixing it is also a precondition for [the redesigned gesture set](../discussion/webapp.md#id/BlockNode:00CMWMP6X8001), where ctrl must work uniformly on the row, the arrow and the stem.
- **A checkbox's checked state never reaches any renderer, console or webapp**: <a name='id/BlockNode:00CMXJQX1R001' class='aperas-anchor aperas-id'></a> `astParser.ts:705-706` stores it correctly as a `checked` prop and strips the literal `[ ]`/`[x]` from `.text`/`.title` so it isn't duplicated as text, and `project.ts` re-emits it correctly on the way back to `.md` — but `RenderNodeItem` (`node.ts`) has no `checked` field at all, and `buildNodeItem` never reads the prop, so every reader downstream of the render tree sees a plain listItem with no marker anywhere: not `aperas tree`, not `aperas unfold`'s own preview, not the webapp. The round trip through disk is correct; only the *reading* path drops it, the mirror image of [the table/code gap above](#id/BlockNode:00CMX4NCW8001) — that one loses structure already present in the abstract string, this one never puts the value in the string's carrier (`RenderNodeItem`) to begin with. Fix is additive: a `checked?: boolean` field, read via `getProp` the same way `holder`/`starred` already are, surfaced as a real glyph rather than folded into title text (a `- [ ]`/`- [x]` prefix would collide with `.fd-title`'s own bold styling the way it collides with nothing today).
- **A table/code block's own row title is a raw snowflake, not a readable name**: <a name='id/BlockNode:00CQ0KYYQG000' class='aperas-anchor aperas-id'></a> `table`/`code` blocks have no extractable lead-in term, so `astParser.ts`'s fallback title (the block's own snowflake id) is what the UI prints as the row's name. Split out from [the block-rendering bug it was originally filed under](#id/BlockNode:00CMX4NCW8001) once that bug's other two causes were fixed — a separate, server-side naming concern with no clear fix identified yet, not the same shape of problem.

## Pending Tasks <a name='id/BlockNode:00CJ6V6370005' class='aperas-anchor aperas-id'></a>

- **Build the tick channel**: <a name='id/BlockNode:00CJ8FEVP0000' class='aperas-anchor aperas-id'></a> streaming NDJSON endpoint plus a `fetch()` + `ReadableStream` client, per the Settled transport decision.
- **Finish "the two link sites" (Slice 6's remaining half)**: <a name='id/BlockNode:00CJ8FEVP0001' class='aperas-anchor aperas-id'></a> the link text (`cite-ref`) and the endnote row (`cite-note`) both render correctly and independently, and the hover popover can promote a link into `unfolds` — but the two sites aren't cross-linked: no numbered back-reference, no click-through from marker to endnote or back, and a target cited twice has no `^ a b`-style disambiguation between its two occurrences yet.
- **Finish shallow write's link-sweep surfacing (Slice 8's remaining half)**: <a name='id/BlockNode:00CJ8FEVP0002' class='aperas-anchor aperas-id'></a> the edit itself round-trips correctly (verified byte-identical through project + an independent re-parse), but a write whose links fail to persist — the known hazard `issues/linking.md` tracks — isn't surfaced in the UI at all; `/api/update` only ever returns `{ok:true}`.

## Resolved <a name='id/BlockNode:00CJ6V637000F' class='aperas-anchor aperas-id'></a>

- **Resolved: fold/unfold felt like seconds per click**: <a name='id/BlockNode:00CJ840278001' class='aperas-anchor aperas-id'></a> the dev API bridge shelled out a fresh `tsx packages/cli/src/aperas.ts <verb>` subprocess per request — a full Node-plus-TypeScript-transpile startup on every click, on top of the service round trip that was already fast. Fixed by moving the bridge into its own long-lived process (`packages/web/devApiServer.ts`, spawned once by `vite.config.ts`) that talks to the ApeironNgn service directly over its unix socket, the same `request`/`ensureServiceRunning` every `kg*.ts` entrypoint already uses. A second, independent cause compounded it: `flush: true` on the `unfold`/`fold` service calls forced a full ~1600-node content-mirror dehydrate to disk on every click, because `service.ts`'s own 'unfold'/'fold' cases set the content-`dirty` flag unconditionally (a defensive catch-all for the rare case where resolving a bare `--view` mints a first-use `Profile`). `TreeView`'s own fold state is genuinely ephemeral UI state with its own independently-tunable flush cadence (design/webapp.md's Persistence) — it needs no per-click disk write at all. Fixed by dropping `flush` from the dev bridge's `unfold`/`fold` calls. Timed before/after: ~450ms per click down to ~10ms once both fixes landed — nothing here was ever a SolidJS cost.
- **Resolved: Slice 1 — `buildRenderTree` extracted into `@aperas/core`**: <a name='id/BlockNode:00CJ8FJNKR001' class='aperas-anchor aperas-id'></a> verified against the existing suite plus a live before/after byte-diff on a real corpus view, and again exhaustively against the UI — every one of 36 rendered rows' fold counts matched `aperas tree --view`'s own `[+N]` tags in sequence, extracted straight from the live DOM.
- **Resolved: Slice 5 — `FolderDiv` ported to Solid**: <a name='id/BlockNode:00CJ8FJNKR002' class='aperas-anchor aperas-id'></a> fold/unfold writes to the real `TreeView.unfolds` set and refetches; verified exhaustively against the CLI (see Slice 1's own entry, same check covers both).
- **Resolved: Slice 7 — zoom**: <a name='id/BlockNode:00CJ8FJNKR003' class='aperas-anchor aperas-id'></a> ctrl-click and breadcrumbs both change apex via the same code path a deep-linked URL uses; confirmed live via the browser and via URL query-param navigation.
- **Resolved: bare-path corpus links (no `#id/...` fragment) rendered inert in the UI**: <a name='id/BlockNode:00CJAW3BS0001' class='aperas-anchor aperas-id'></a> `design/linking.md`'s Context section links `**Discussion**`/`**Planning**` by plain relative path with no fragment at all — a form the graph's own resolver never even attempted to resolve until the same-day core fix (see [History](../history/linking.md#id/BlockNode:00CJAVGNJG001)). Resolving it in the graph wasn't enough on its own: `Inline.tsx` has no server round trip of its own to learn a link's target, so it still needs a `#id/<Kind>:<id>` fragment sitting in the href text itself. Fixed with `bakeResolvedLinkIds` (`apeironNgn/node.ts`), which bakes each already-resolved bare-path link's target into its href for the render tree only — the stored text and every projected file stay untouched. Verified live in a real browser: clicking both links now navigates to their target `ArtifactNode`, breadcrumbs and content updating correctly.

- **Resolved: Slice 2 — per-run auth token**: <a name='id/BlockNode:00CNSME9Z8001' class='aperas-anchor aperas-id'></a> `randomBytes(32).toString('hex')`, generated once at service startup (`service.ts#main`) and written to the run directory (`serviceLock.ts#writeToken`) at mode 0600, alongside the lock and socket it already trusts. Verified per plan: file exists at 0600, and a service restart rotates it (a request bearing the old value is rejected once the new service is up).
- **Resolved: Slice 3 — HTTP listener**: <a name='id/BlockNode:00CNSME9Z8002' class='aperas-anchor aperas-id'></a> the route table itself (`apiRoutes.ts`) is shared with the dev-only bridge (`packages/web/devApiServer.ts`) rather than duplicated — one op allowlist, dispatched in-process via `handle()` (`service.ts`) for the production listener, or over the unix socket via `request()` for the dev bridge. Auth (`listenerAuth.ts`): loopback-only `Host`, an `Origin` allowlist, and `X-Aperas-Token` compared with `timingSafeEqual`; a restrictive `OPTIONS` preflight response (no `Access-Control-Allow-Origin` for anything but the listener's own origin) is what actually stops a hostile cross-origin page's real request from ever being sent. Verified adversarially: no token, wrong token, foreign `Origin`, and non-loopback `Host` each rejected (401/401/403/400); an op outside the allowlist 404s even with a valid token; a same-origin call with a valid token and matching `Origin` succeeds and returns real graph content.
- **Resolved: Slice 16 — packaging**: <a name='id/BlockNode:00CNSME9ZG000' class='aperas-anchor aperas-id'></a> static asset serving added to the same listener (`staticServe.ts`), `index.html` getting the current token injected server-side so the page's own same-origin JS can read it (`window.__APERAS_TOKEN__`); the webapp's `apiFetch()` helper attaches it to every `/api/*` call, a no-op in dev where the token is undefined. New `aperas serve` subcommand (`kgServe.ts`) resolves the graph from cwd, ensures a service is running for it, and prints the listener's URL. `build.mjs` now also runs `vite build` in `packages/web` and copies its `dist/` to `dist/web/`, sibling to the CLI bundle, so the packaged binary is self-contained. Verified end-to-end against a throwaway second graph (its own `aperas.config.json`, distinct name and content) with the AperasKG-bound service stopped first: the packaged `aperas serve` showed the throwaway graph's own name and content, fold/unfold/edit worked against it, an external CLI edit was picked up by the existing poll through the new listener, and every Slice 3 adversarial check was re-run against the packaged instance specifically rather than assumed from source-checkout testing. Surfaced one real bug along the way — `resolveEffectiveGraphName()` (`graphConfig.ts`) resolved from the *service process's own* cwd, which for a packaged binary is wherever it's installed, never the graph's directory; it silently reported "AperasKG" while genuinely serving the throwaway graph's data. Fixed by adding the same `APERAS_GRAPH_NAME` env-var passthrough `APERAS_APEIRON_ROOT`/`APERAS_ARTIFACTS_ROOT` already had, resolved once by `bindAndSpawn` (`kgService.ts`) from *its own* correct cwd and threaded through `spawnService`.
- **Resolved: the webapp's 1s poll remounted the entire tree**: <a name='id/BlockNode:00CNRB3A90001' class='aperas-anchor aperas-id'></a> `<For>` reconciles by object reference, and a plain `createResource` handed it a fresh object graph on every poll — so it tore down and remounted every row each second, dropping any open inline editor mid-edit. Fixed with `reconcile(data, {key: 'id'})` (`solid-js/store`), merging each poll's response into a persistent store by node id. Unblocked by renaming `RenderLinkItem.linkId` to `id` in `core` — an unmotivated naming split with no functional reason, found while auditing for a uniform reconcile key. Verified live in a browser: 45 tagged DOM nodes and a mid-edit, unsaved textarea both survived 4 poll ticks with no remount. Full investigation: [Freeflow](../discussion/webapp.md#id/BlockNode:00CNR2E3D0001).
- **Resolved: the production webapp's hover link-preview never resolved, stuck on "Loading…"**: <a name='id/BlockNode:00CPYRQN40001' class='aperas-anchor aperas-id'></a> `Inline.tsx`'s `LinkPopover` fetched `/api/tree` with a plain `fetch()`, never `apiFetch()` — unauthenticated against the production HTTP+auth listener (Slice 3), so it got a 401 with a JSON error body. `fetch()` doesn't reject on HTTP error statuses, and this call never checked `res.ok`, so the 401's `{error: ...}` body was parsed as if it were a real `TreeResponse`; `body.tree` was silently `undefined`, `preview()` never settled and `error()` never got set, leaving the loading guard (`preview() === undefined && !error()`) true forever. Predates Slice 3's auth work entirely — never exercised end-to-end against the real production listener until now. Fixed to match every other fetch in the app: `apiFetch()` plus a `res.ok`/`body.error` check. Verified live against the built package's own listener (not dev): dispatched a real `mouseenter` on a corpus link and confirmed the popover rendered its actual title/abstract instead of hanging.
- **Resolved: every block-structured node rendered as one flattened run of literal markup in the UI — tables and fenced code were unreadable**: <a name='id/BlockNode:00CMX4NCW8001' class='aperas-anchor aperas-id'></a> a `[table]` showed as `| a | b | | --- | --- | | 1 | 2 |` in flowing prose, and a `[code]` block showed its own ``` fences as text with every line rewrapped, destroying column alignment. Two causes, both fixed: every `RenderNodeItem.abstract` is a one-line preview with all whitespace collapsed (`truncateForPreview`, `astParser.ts`), and `Inline.tsx` was deliberately inline-only with no block-level renderer at all. [AnDT1211's `feat/web-markdown-rendering`](../history/webapp.md#id/BlockNode:00CPYRTRA0001) added real block-level renderers (`MarkdownTable.tsx`, `CodeBlock.tsx`) that read a node's own full `.text` instead of the collapsed `.abstract`. Verified live against this exact corpus's own [fold/unfold redesign](../discussion/webapp.md#id/BlockNode:00CMWMP6X8001) content, the issue's original worked example: a real 6-row `<table>` and a code block with preserved line breaks and alignment, confirmed via direct DOM inspection. A third cause originally filed here — the row's own title falling back to a raw snowflake id — has no clear fix yet and isn't the same shape of problem, so it's split out as [its own open issue](#id/BlockNode:00CQ0KYYQG000).
