# Core — Planning <a name='id/BlockNode:00CF23FB1G001' class='aperas-anchor aperas-id'></a>

## Implementation Plan <a name='id/BlockNode:00CF23FB1G002' class='aperas-anchor aperas-id'></a>

Fix by shared root cause, not by filing order. Three of these [Open Issues](../issues/core.md#id/BlockNode:00CF1WZ4JG002) are explicitly "flagged from reproduced symptom, not source" — `reconcile.ts`'s actual matching logic hasn't been read yet — so investigation comes before any diff, not interleaved with it. Group A shares one file and plausibly one fix; item 6 shares its shape of fix (validate everything before writing anything) with `cli`'s own item 5, even though the two now live in separate concerns; the seventh item is a missing feature, not a bug, and is deferred alongside the (separately deferred) CRUD work rather than forced into this batch.

## Task Breakdown <a name='id/BlockNode:00CF23FB1G003' class='aperas-anchor aperas-id'></a>

1. **Investigate, read-only**: <a name='id/BlockNode:00CF23FB1G004' class='aperas-anchor aperas-id'></a> read `packages/core/src/reconcile.ts` in full, and the relevant parts of `packages/core/src/apeironNgn/node.ts`, to confirm the actual root cause behind [items 1](../issues/core.md#id/BlockNode:00CF1WZ4JG003), [2](../issues/core.md#id/BlockNode:00CF1WZ4JG004), and [4](../issues/core.md#id/BlockNode:00CF1WZ4JG006) — currently symptom-only — and check whether they genuinely share one fix with [item 3](../issues/core.md#id/BlockNode:00CF1WZ4JG005) or are subtly different. Report findings and a specific proposed diff before writing any code.
2. **Fix Group A** — [items 1](../issues/core.md#id/BlockNode:00CF1WZ4JG003), [2](../issues/core.md#id/BlockNode:00CF1WZ4JG004), [3](../issues/core.md#id/BlockNode:00CF1WZ4JG005), [4](../issues/core.md#id/BlockNode:00CF1WZ4JG006) — `reconcile.ts`'s identity-matching family. Verify against the reproducible scenarios already on record: the eval-sandbox repro for item 1 (push against `issues/treeview.md`'s Open Issues list with a tombstoned sibling interleaved), `list-consumption`'s repro for item 2.
3. **Fix item 6** — [multi-artifact `kg:ingest` isn't transactional](../issues/core.md#id/BlockNode:00CF1WZ4JG008) (`apeironNgn/artifacts.ts`: <a name='id/BlockNode:00CF23FB1G006' class='aperas-anchor aperas-id'></a> validate every artifact in a batch before committing any of them). The matching single-`kg:insert` gap (`kgInsert.ts`) moved to `cli`'s own planning doc along with the issue itself — same shape of fix, different concern now.
4. **Defer item 7** ([Folder/Artifact holder-promotion gap](../issues/core.md#id/BlockNode:00CF1WZ4JG009)) — no design exists yet for what promotion should look like; revisit alongside the CRUD work once that's picked back up, not as part of this batch.
5. Move each fixed item from `issues/core.md`'s Open Issues to Resolved as it lands, with the real root cause recorded — not just "fixed."

## Verification Plan <a name='id/BlockNode:00CF23FB1G009' class='aperas-anchor aperas-id'></a>

Each Group A fix is checked against its own already-documented repro, not just a fresh test written for the occasion — item 1 against the exact `issues/treeview.md` list shape that exposed it, item 2 against `list-consumption`'s eval sandbox. Item 6's fix is checked by forcing the failure directly: fail a later file mid-batch `kg:ingest` and confirm earlier files' writes didn't commit (the matching single-`kg:insert` check now lives in `cli`'s own planning doc). A fix isn't done until its Open Issues entry can honestly move to Resolved with the actual mechanism named, matching this concern's own citation-direction discipline against guessing.
