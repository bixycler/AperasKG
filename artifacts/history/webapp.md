---
description: Where the webapp actually stands: Slice 1 and the UI (Slices 5-8) have a real working dev build; the auth token, HTTP listener and tick channel (Slices 2-4) are still not built.
---

# Webapp — History <a name='id/BlockNode:00CJ6V6N9R001' class='aperas-anchor aperas-id'></a>

## Current Status <a name='id/BlockNode:00CJ6V6N9R002' class='aperas-anchor aperas-id'></a>

Slice 1 is built. The UI itself (Slices 5-8: `FolderDiv`, links at two sites, zoom, shallow write) also has a real, working dev build — running against the live graph through a dev-only bridge (`packages/web/devApiServer.ts`), not the production listener. Not built: the per-run auth token, the real HTTP listener, and the tick channel (Slices 2-4) — the dev bridge talks to the ApeironNgn service directly over its unix socket instead, which is honest for local development but has none of the auth or the live-push story a shipped listener needs. The dev bridge defaults to fixed ports rather than Vite's own default or an ephemeral one: the Vite dev server listens on 2737, the bridge on 2734, both spelled out in `packages/web/vite.config.ts` and derived from the project names — `aper` from Aperas, `apei` from Apeiron. In place of the still-missing tick channel, the dev tab polls its own refetch every 1s (a `sync` checkbox in the header turns it off), which is what makes an external `aperas fold`/`unfold`/CLI write show up without a manual reload — see [why this is explicitly a stopgap, not the settled push design](../design/webapp.md#id/BlockNode:00CJDHHVA8000). `FolderDiv`'s own render polish also landed: [a compact glyph per node kind](../design/webapp.md#id/BlockNode:00CJDHWT9G001) in place of the old `[FolderNode]`/`[heading]`/... bracket, [the fold arrow plus its guide line unified into one hoverable/clickable control](../design/webapp.md#id/BlockNode:00CJDHYES8001), [a link row now shows its target's own kind glyph and splits title from abstract the same way an unfolded node does](../design/webapp.md#id/BlockNode:00CJE14W0R001), [body text lost the dimming it never needed](../design/webapp.md#id/BlockNode:00CJE11FRR000), and [each node's title line now shows its own backlink count, opening a popover of them on click](../design/webapp.md#id/BlockNode:00CJEDTR40001).

## Milestones <a name='id/BlockNode:00CJ6V6N9R003' class='aperas-anchor aperas-id'></a>

- **2026-09-17: webapp design opened**: <a name='id/BlockNode:00CJ6V6N9R004' class='aperas-anchor aperas-id'></a> scope fixed to the UI-projection tier, a third thin wrapper over `@aperas/core` alongside `cli`. Settled that the webapp never owns fold state, that `FolderDiv`'s three slots are already the three render tiers, that the apex is UI-local rather than view state, and that the write boundary is deep read with shallow write for principled reasons rather than as a scope cut.
- **2026-09-18: transport, auth and the render contract settled**: <a name='id/BlockNode:00CJ6V6N9R005' class='aperas-anchor aperas-id'></a> an HTTP listener inside the ApeironNgn service rather than a separate bridge or a Node-hosted shell, chosen because live notification can only honestly originate in the sole writer. Change notification is a bare tick with the UI refetching its apex, carried as NDJSON over a streaming `fetch()` response read with `ReadableStream` — `EventSource` rejected because it cannot set a request header, and its resumption machinery is worthless against an idempotent tick. Auth resolved to five controls with the op allowlist as the load-bearing one. The superscript link marker was dropped in favour of the link text itself, and the canonical-position verdict was committed to `@aperas/core` as `buildRenderTree`, so both link sites and the CLI read one shared computation.
- **2026-09-18: a real dev build, clicked through end to end**: <a name='id/BlockNode:00CJ83NV3G001' class='aperas-anchor aperas-id'></a> `packages/web` renders the live corpus via a Solid `FolderDiv`, driven entirely by real graph state through a dev-only bridge to the ApeironNgn service (talking to it directly over its unix socket — the same `request`/`ensureServiceRunning` every `kg*.ts` entrypoint already uses — rather than shelling out a CLI subcommand per click, which the first version of this bridge did and which made fold/unfold feel like seconds-per-click; a `flush: true` inherited from that first version separately forced a full content-mirror dehydrate on every fold/unfold too, since `service.ts`'s own cases set the content-dirty flag unconditionally). Working end to end and confirmed against the live graph, not just the UI: fold/unfold writes to the real `TreeView.unfolds` set; ctrl-click zoom and breadcrumbs change apex; prose is rendered as real markdown with navigable inline links (resolved via the same `#id/<Kind>:<snowflake>` anchors design/linking.md already writes); a hover popover previews a link's target and can promote it into `unfolds`; the edit pencil is Slice 8's shallow write, piping straight to `aperas update --text-only` and verified round-tripping type/parent/children unchanged.
- **2026-09-18: bare-path corpus links made navigable in the render tree**: <a name='id/BlockNode:00CJAVRF90001' class='aperas-anchor aperas-id'></a> [design/linking.md's bare relative-path form](../history/linking.md#id/BlockNode:00CJAVGNJG001), fixed the same day, resolves in the graph now — but the webapp's `Inline.tsx` has no server round trip of its own to learn a link's target; it only ever recognizes a `#id/<Kind>:<id>` fragment already sitting in the href text, so a correctly-resolved bare-path link still rendered inert. Fixed with `bakeResolvedLinkIds` (`apeironNgn/node.ts`): before truncating a node's `abstract`/a link's `targetAbstract` for the render tree, it splices `#id/<target>` onto any `./`/`../`-leading href, using each `Link`'s own stored occurrence `position` — render-tree-only, the stored text and every projected file stay untouched. Verified live in a real browser: clicking `design/linking.md`'s `**Discussion**`/`**Planning**` links now navigates to their target `ArtifactNode`s, breadcrumbs and content updating correctly.
- **2026-09-24: production listener's port fixed to follow the project's own naming convention**: <a name='id/BlockNode:00CP9R7P50001' class='aperas-anchor aperas-id'></a> `aperas serve`'s HTTP+auth listener (Slices 2-3) defaulted to `4173` — Vite's own `vite preview` port, carried over incidentally rather than derived from anything project-specific, unlike the dev bridge's `2737`/`2734`. Changed to `2736`, continuing the same phone-keypad-spelling scheme: `apeo` (2=A, 7=P, 3=E, 6=O), from Ape[ras] + [Apei]o[n] — deliberately distinct from the dev ports so a dev server and a served instance can run side by side against the same graph without colliding. `serviceLock.ts#resolveHttpPort`.

- **2026-09-25: markdown rendering merged from AnDT1211's `feat/web-markdown-rendering`**: <a name='id/BlockNode:00CPYRTRA0001' class='aperas-anchor aperas-id'></a> four new renderer components — `CodeBlock.tsx`, `MarkdownTable.tsx`, `MarkdownQuote.tsx`, `MermaidDiagram.tsx` — so a node's own prose renders fenced code, tables, blockquotes, and Mermaid diagrams instead of literal markdown syntax, wired into `FolderDiv.tsx`'s content rendering. Same PR also added `fetchTree`'s own response-dedup cache (skip re-parsing/re-returning an identical `/api/tree` body across polls), `fetchGraphInfo`/`/api/graph` (the graph's display name, consumed by `App.tsx`'s `document.title` effect), and root-level `npm run dev`/`build:web` convenience scripts delegating to `packages/web`'s own. Merged via a rebase of this branch onto `main`; the conflicts (`RenderLinkItem`'s `id`-vs-`linkId` rename against this PR's added `text` field, and `fetchTree`'s caching against this branch's `apiFetch`/`fetchGraphInfo` additions) were both genuinely independent, non-competing changes — resolved by keeping both sides at every site, not by picking one.
- **2026-09-25: the hover link-preview auth bug (above) found and fixed**: <a name='id/BlockNode:00CPYRTRA0002' class='aperas-anchor aperas-id'></a> surfaced only once the merged tree was actually exercised against the real production listener in a browser — `Inline.tsx`'s pre-existing `LinkPopover` fetch predates Slice 3's auth work entirely and was never updated to match it. See [issues](../issues/webapp.md#id/BlockNode:00CPYRQN40001) for the full root cause.
- **2026-09-25: every hand-rolled markdown parser replaced with one shared `remark-gfm` AST**: <a name='id/BlockNode:00CQ1BGR2R001' class='aperas-anchor aperas-id'></a> `MarkdownTable`/`MarkdownQuote`/`CodeBlock`'s fence detection/`Inline.tsx`'s inline tokenizer were four independent regex/string-walking re-implementations of the same spec `core`'s own `astParser.ts` already parses correctly — a real correctness risk (two independent decisions about what's a valid GFM table, free to disagree), not just duplication. New shared `markdown.ts` parses once via `unified`+`remark-parse`+`remark-gfm`; `classifyBlock` reads the resulting AST to detect table/quote/code/Mermaid instead of a bespoke sniffer per kind. Real wins beyond consistency: GFM strikethrough, nested formatting inside link text, and autolinks now render correctly, none of which the old tokenizer recognized; Mermaid detection is now `code.lang === 'mermaid'` instead of a diagram-type keyword list that didn't cover every type. `CodeBlock.tsx`'s own hand-rolled 5-language highlighter was also swapped for `highlight.js` (core + 21 explicitly registered languages, not the full ~190-language barrel — the 6 this corpus already uses plus 15 more common ones added ahead of need), since the old one had no entries for `json`/`rust`/`markdown`/`text` at all; an unregistered language now degrades to plain text rather than `highlightAuto` guessing among the registered set (confirmed live to mislabel Python as bash before Python was added). Both `MarkdownTable` and `CodeBlock` gained a Copy button; the table's is view-aware (TSV in "Table" view, Markdown in "Source" view). See [issues](../issues/webapp.md#id/BlockNode:00CQ0VT1NR001) for the full resolution.
